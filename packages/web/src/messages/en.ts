/**
 * Catalogue en — traductions depuis fr.ts. Les clés absentes retombent
 * automatiquement sur le français (voir i18n.tsx). NE PAS laisser de clé vide.
 */
import type { Messages } from '../i18n'

export const en: Messages = {}
